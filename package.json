{
  "name": "claude-code-practice",
  "version": "1.0.0",
  "description": "Practice project for Claude Code + GitHub workflow (commit, push, Actions, PR)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "dev": "tsx src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "tsx": "^4.19.2"
  }
}
